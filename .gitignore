# Dependencias de Node (¡Son muy pesadas y se instalan con npm install!)
node_modules/

# Variables de entorno y secretos (¡CRÍTICO POR SEGURIDAD!)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Logs de errores de NPM
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Archivos de sistema operativo (Basura común)
.DS_Store
Thumbs.db

# Configuración de IDEs (Opcional, pero recomendado si trabajas en equipo)
.vscode/
.idea/

# Carpetas de build o distribución (si compilaras el proyecto en el futuro)
dist/
build/
coverage/